/**
 * Meta + TikTok conversion eventleri — şimdilik devre dışı, sonra tekrar eklenecek
 */
export function trackCompleteRegistration(): void {
  /* Meta/TikTok kaldırıldı */
}

export function trackViewContent(_contentType: string, _contentId?: string): void {
  /* Meta/TikTok kaldırıldı */
}

export function trackSubscribe(_value: number, _currency: string): void {
  /* Meta/TikTok kaldırıldı */
}
